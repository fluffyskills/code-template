import React from 'react'
import ImageCollection from './ImageCollection/ImageCollection'
import NewsFeed from './NewsFeed/NewsFeed' 
import FeatureCard from './Common/FeatureCard'

 const Layout = (props) => {
  return (
      <div className="Layout-container">
        {/* simple dashboard where we need to discuss a proper layout after Components are builf */}
        {/* one click blog  */}
        {/* social card preview w/ acct integration */}
        {/* news-feed highlights related to blogger */}
        {/* voice to text  */}
        {/* image-api wirh previews related to article */}
        {/* idea tab to document ad-hoc thoughts that you have no time to write a post, that auto-remind nexr sifn in*/}
        {/* MenuComponent */}
        {/*  */}
        {/*  */}
        {/*  */}

      </div>
  )
}

export default Layout