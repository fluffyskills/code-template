import React from 'react'

const FeatureCard = (props) => {
  return (
    <div className="FearueCard-container">
      FeatureCard:
      <h3>
        {this.props.featureTitle}
      </h3>
      <div className="feature-list"></div>
      <ul>
        <li>{this.props.featureTopic}</li>
      </ul>
      {/* shared card that maps to each feature component to generate the conrent on the dashboard */}
    </div>
  )
}

export default FeatureCard bvm 
:p  xpkj z